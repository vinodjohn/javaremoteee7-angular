export class Dog {
  name: string;
  type: string;
  property: string;

  constructor(name: string, type: string, property: string) {
    this.name = name;
    this.type = type;
    this.property = property;
  }
}
